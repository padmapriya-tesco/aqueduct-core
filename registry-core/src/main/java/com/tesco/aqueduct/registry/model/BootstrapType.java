package com.tesco.aqueduct.registry.model;

public enum  BootstrapType {
    PROVIDER,
    PIPE_AND_PROVIDER,
    PIPE,
    PIPE_WITH_DELAY,
    PIPE_AND_PROVIDER_WITH_DELAY,
    CORRUPTION_RECOVERY,
    NONE
}
